#include "main.h"
/**
 * swap_int : a function to swap the values of two integers.
 *
 *@a : the first entry
 *@b : the second entry
 *
 */
void swap_int(int *a, int *b)
{
	int c;

	change = *a;
	*a = *b;
	*b = change;
}
